# Milestone 6: Hardening & v1.0 — Battle-Tested

**Status:** active
**Started:** 2026-08-15, the day Milestone 5 was audited and archived

Completed milestones are archived under `docs/planning/milestones/`. Milestone 5 was archived
there on 2026-08-15 after its close audit — `docs/plans/2026-08-15-milestone-5-audit.md`, verdict
PASS on all five criteria.

> **The ROADMAP is authoritative for the DoD below; the two must agree, and when they do not, this
> file is the one that is wrong.** This file drifted twice in Milestone 4/5 and was caught by an
> audit both times; it is rewritten at every milestone boundary for that reason.

> **This milestone was re-planned before it opened**, at the operator's request on 2026-08-15 —
> *"make Milestone 6 about testing every available feature so we are battle-tested"* — from
> `docs/plans/2026-08-15-milestone-6-battle-tested-replan.md`. The ROADMAP's Milestone 6 section
> is rewritten from that note. **Both of the note's operator decisions are now closed, and they
> closed differently.**
>
> **The recordings question was decided on 2026-08-20, against the note's recommendation.** The
> note proposed that `<VerifiedByReason>` carry the gap where no credentials exist, on the grounds
> that a criterion satisfiable only by credentials that may never arrive is not falsifiable. The
> operator decided instead that **6.1's recordings do gate v1.0**, with 6.1's *work* postponed
> behind 6.2.3. Recorded with the trade-off attached because it was raised at the time and
> accepted: v1.0 now waits on 18 cassettes whose blocker is accounts rather than effort, so if
> those accounts do not arrive, the tag does not either. `<VerifiedByReason>` remains implemented
> and enforced; it is simply no longer the answer for the connectors.
>
> **The #247 / #239 question was settled by events rather than by a decision**: both fixes are on
> `main` as of 2026-08-18 (#311, #312; #296, #291), so they ship in v1.0 unless someone reverts
> them. Noted as such because a decision nobody made is worth distinguishing from one that was
> taken.

## Goal

Every shipped feature exercised for real — a real model, a real corpus, a real store, a real
file, a real service or a recorded one — with the evidence pinned and checked by a test, before
v1.0. Milestone 5 showed why: `Rag.NET.GraphRag` was `VerifiedBy=unit`, `✅ Done`, green, published,
and running it once found eight defects. The dense path, calibrated against four published figures,
is the counter-example: every defect ever found in it was found by that calibration.

## Definition of Done

Authoritative copy in the ROADMAP's Milestone 6 section, in Phase 4.0's falsifiable style.

- [x] **Milestone 5 complete** — closed 2026-08-15 by audit, verdict PASS.
- [ ] **All planned phases complete** — what remains is **three**: 6.1 Recorded Responses
      (blocked on accounts, still gating the tag), **6.2.43 the fluent entry point (pending, added
      2026-09-12 from #184 — and scoping found its premise had drifted: the builder is already
      fluent, so the phase closes one seam rather than redesigning an entry point)**, and 6.3
      Release v1.0. **6.2.42 the mechanical guards closed 2026-09-12 in #572.** **6.2.41 the MTP migration closed 2026-09-12 in #567** — though not for the reason it was scoped: #314 stays open, now
      correctly attributed to SDK support rather than to this repository. **6.2.40 the prompt-injection documentation (#552) closed
      2026-09-11 in #561.** 6.2.41 is the first item in a while that came from a red CI check rather
      than from a phase finding it — #314 had been failing since 2026-08-18, diagnosed only when
      someone asked why. **6.2.39 the model boundary closed
      2026-09-11 in #556** — and produced 6.2.40 twice over: its posture section surfaced #552, and
      its own cross-reference turned out to point at a design proposal marked Done. **6.2.38 the security posture closed 2026-09-11 in #553**, and produced
      6.2.39 by making an omission visible — which is the posture earning its keep rather than the
      count drifting. **6.2.37 the decorator that hid a capability (#544) closed 2026-09-10 in
      #549**, filed, scoped, built and merged inside a day. **This count has now moved four times in
      two days, and 6.2.38 is the one that should not have needed moving**: it was recorded as open
      on 2026-09-07 and simply never scheduled, so every "what remains" written since has been
      undercounting work that was written down in the same directory. **This line said "two" for about an
      hour** on 2026-09-10 — written when 6.2.36 closed and true only until #544, which 6.2.36 had
      itself filed hours earlier, was scoped into a phase. **That is the record-then-schedule rule
      working, not drifting**: the count went up because a recorded finding became scheduled work,
      which is the whole point of recording it. It has now read two, three, two and three inside one
      day. **6.2.36 the semantic ranker's move to the hybrid path (#539) closed 2026-09-10 in
      #545**, filed and fixed the same day. **This line said "two" for four hours** on 2026-09-10,
      between 6.2.35 closing and #539 arriving, then said "three" for eight more; the shortest any
      version of it has been true, and a fair reminder that "what remains" counts planned work, not
      defects not yet found. It is back to two by closing the defect, not by discounting it. **6.2.35 the benchmark filter
      guard (#529) closed 2026-09-10 in #540**, the first phase since 6.2.30 with nothing
      account-blocked. **Both survivors are now blocked on the same thing**, and neither is blocked
      on effort: the tag waits on 6.1's cassettes, and 6.1 waits on accounts. **6.2.34 the Azure semantic
      ranker (#328) closed 2026-09-10 in #536** — it shipped *with* its account constraint rather
      than waiting for one, by guarding the unverifiable path with a throw instead of a claim; the
      remaining gap is recorded in `<VerifiedByReason>`, not left implicit. **Corrected 2026-09-09 — the fourth drift of this line.** It said "6.2.1
      Retrieval & Answer Sweep (active)" for three days after that phase closed on 2026-09-06, and
      it listed none of 6.2.13–6.2.30, which is nineteen phases. The Phases table below stayed
      correct throughout: it is edited by `complete-phase`, and this prose line is not, so the two
      drift by construction rather than by neglect. **Read the table, not this line.**
      Complete: ~~6.0 Inventory~~ (2026-08-15), ~~6.2 Raise the Floor~~
      (substantially, 2026-08-17), ~~6.2.2 Requested Features~~ (2026-08-16), ~~6.2.3 Corpus-Level
      RAPTOR~~ (2026-08-21), ~~6.2.4 RAPTOR Retrieval Over-Fetch~~ (2026-08-21), ~~6.2.5 Contract
      Defects~~, ~~6.2.6 Package Boundaries~~, ~~6.2.7 Named Pipelines~~, ~~6.2.8 Requested DX and
      Chunking Quality~~, ~~6.2.9 `Umap.Fit` at Corpus Scale~~, ~~6.2.10 Vector-Store
      Initialisation~~, ~~6.2.11 HTML Structure and a Guid Seam~~ (all 2026-08-25), ~~6.2.12
      Dogfooding Defects~~ (2026-08-26).
      *(This line listed 6.2.5–6.2.10 as outstanding until 2026-08-27, five days after their
      code was on `main`, and never listed 6.2.11 or 6.2.12 at all — the third time this file
      has drifted from the ROADMAP. A status is written when a phase is planned, and nobody is
      editing this file at the moment its PR merges.)*
      *(6.2.5–6.2.9 added 2026-08-25 from the GitHub backlog, pre-tag on the operator's
      decision. Consistent with 6.2.2's charter: this is the terminal milestone, so a
      request filed against a published package has nowhere later to land.)*
- [ ] **Every `✅ Done` row in `features.md` names what exercises it** — an *Exercised by* column,
      pointing at a test or benchmark that runs the real thing, and a conventions test that fails a
      ✅ row with an empty column. Today: 56 rows, 0 pointers.
- [ ] **No package remains at bare `VerifiedBy=unit`** — each is `integration`, `container`,
      `recorded`, `benchmark`, or carries `<VerifiedByReason>` naming the service and the gap; the
      ledger test fails a bare `unit`. Today: **22 of 73**, down from 57 when 6.0 wrote the list.
      Of the 73: 29 `integration`, 24 `unit` (22 bare, 2 with a reason), 11 `container`, 5
      `benchmark`, 1 `recorded`, 1 `live`. `recorded` and `live` were both used for the first time
      on 2026-08-17 — `Rag.NET.DataProviders.GitHub` and `Rag.NET.Parsers.Vision`.
      *(`integration` was added 2026-08-16 in Phase 6.2. The level set enumerated here could not
      express "exercised against something real that is not an external service" — a real file, a
      real process, a real host over its real transport, a real store reopened — which is exactly
      what §2 of the 6.2 design defines as the bar for twenty-five of these packages. They met it
      and the ledger had no way to say so. Amending a DoD mid-milestone is not free, and the
      alternative was worse: leaving twenty-five packages at a `unit` that had become false, or
      spending the `<VerifiedByReason>` escape hatch on packages that are exercised.)*
- [ ] **Every retrieval technique and answer engine has a pinned figure with a control** — the
      GraphRAG method (5.2 / 5.2.1 / 5.2.2) applied to RAPTOR, HyDE, hybrid, reranking, late
      chunking, SPLADE, and the three answer engines; every vector store reproduces the SciFact
      parity figure through itself; a pipeline-parity test holds a real `AddRagNet` pipeline to the
      harness's top-k on every push.
- [ ] **The release commit is green on both `ci.yml` matrices**, the Docker tier and the latest
      nightly green on Linux, stated as such.
- [ ] **Release tagged v1.0.**

## Phases

| Phase | Name | Issues | Status |
|---|---|---|---|
| 6.0 | The Inventory | — | **complete** 2026-08-15 — both guards on every push, failing behind a work list: 5 packages at `benchmark`, 57 at bare `unit` owned by 6.1/6.2/6.2.1; 51 Done sections, 2 exercised, 49 owned |
| 6.1 | Recorded Responses | #283, #290, #354 | **postponed but moving** — **the first outside cassette landed 2026-08-25** (#354, contributed by @StefH against a real Azure Document Intelligence resource). It is a strict superset of the hand-written fixtures — `paragraphs`, `styles`, `contentFormat`, per-word `polygon` — and hides no defect today, because `BuildPageText` reads only `Pages`→`Lines`/`Words`→`Content`. Still **gating v1.0** — sequenced behind 6.2.3; the gate was kept rather than handed to `<VerifiedByReason>`, so the tag waits on accounts. Previously in progress: the harness did not work and now does. Record mode had two defects, both silent because recording proxies to the real service and therefore passes: recordings were written to a directory replay never read, and every mapping matched on `Host: localhost:{ephemeral port}`, which cannot match twice. Fixed in #290, which also recorded the first working cassette (GitHub, unauthenticated, 17 KB). #283 carries the corrected instructions for the remaining 18 services; the blocker is accounts, not work |
| 6.2 | Raise the Floor on Unit-Only Packages | #286–#292 | **substantially complete** — 57 bare `unit` down to 22. Defined per kind: parsers/chunkers via a real file, stores via the parity leg, utilities via one real run. Every package picked up produced a defect in something adjacent: `Parsers.Audio` was filed as needing a hosted model and needs none (and is broken on Linux without `libgomp1`), `Parsers.Vision` had no CI tier that could run it, `DataProviders.Web`'s crawler yielded the seed page twice (#288). Remaining 22 are 6.1's credential-blocked connectors plus `Chunking.Templates` and 6.2.1's three |
| 6.2.1 | Retrieval & Answer Sweep | — (all four named debts closed) | **complete** 2026-09-06 — every exit-condition clause met, the allowlist clause amended the same day to the phase scope both lists are partitioned by (47 entries remain, all 6.1/6.2, none owned here). Previously active 2026-08-20 — the GraphRAG method applied to the rest. **#247 closed 2026-08-18**, fixed twice over (#311 hides graph chunks from retrieval by default, #312 gives them their own store) and pinned at 0.3494 (#280); #239 and #200 closed 2026-08-17. The local-search thread completed 2026-08-20 (#323, #326). **#345 merged 2026-08-22 in #351 (`bb4c11c7`), unblocking Tasks 4-6 of the RAPTOR measurement** — `TargetClusterSize` floors the cluster count so a level's *average* cluster stays within the model's context; without it the corpus tree could not be built at the shipped default. The sweep itself has not started: RAPTOR first, then HyDE, reranking, hybrid, late chunking, SPLADE, the three answer engines, the stores through the parity leg, the pipeline-parity test, #176, local search's unexplained yes/no abstention, and the now-unblocked deletion of the deprecated blend members. **#176 answered 2026-08-26 in #405** — by naming the dropped relationship endpoints rather than counting them: 565 distinct names, overwhelmingly common nouns and paraphrases, so promoting them into entities would trade a better singleton number for a worse graph. Nothing changed on it; the diagnostic stays. **That closes the last of the phase's four named debts, and the sweep itself is what remains** |
| 6.2.2 | Requested Features | #252 | **complete** 2026-08-16 — #252 built, both open design questions settled, exercised in the fast tier and over a real HTTP server |
| 6.2.3 | Corpus-Level RAPTOR | #331, #332, #333 | **complete** 2026-08-21 — merged in #340. `RaptorIngestionBehavior` clusters `ctx.EmbeddedChunks` and `IngestionContext` holds one document, so RAPTOR builds a per-document tree — #300's shape, and not the paper's mechanism. The fix needs RAPTOR to own persistent leaf-embedding state the way GraphRAG owns `IGraphStore`, because `IVectorStore` cannot enumerate and `IChunkLookup` returns `TextChunk` without vectors (#318). Roughly #302 plus #312. Both scopes stay selectable, per #323's precedent, so the 6.2.1 measurement prices old and new in one run. **Three defects were fixed and three more filed:** #332 (summary chunks collided on `ChunkIndex` across levels) and #333 (`SelectK` returned k=n, so a level never reduced and the tree loop never terminated — an unbounded LLM spend at shipped defaults) were both found by *reading*, not by any test. The reason no test caught them is the phase's most useful finding: a mock embedder re-seeded per call returned a byte-identical vector for every summary, so **no test had ever built a tree deeper than one level**, and both defects need depth ≥ 2 to manifest. Two more fixtures of the same shape turned up while fixing it. Left open and documented: #336, #337, #338 |
| 6.2.4 | RAPTOR Retrieval Over-Fetch | — | **complete** 2026-08-21 — merged in #344, added and closed the same day. `RaptorRetrievalBehavior` passes `ctx` to `next` unmodified while `VectorStoreBehavior` fetches exactly `TopK`, so it only sees the truncated top-k: **`Boost` promotes within the set but never into it** (a summary ranked 7th that would beat 6th at 1.2× cannot appear — #239's shape), and **`Filter` under-fills** (ask for 6, get 3). `MmrBehavior`'s `?? TopK * 3` over-fetch is the pattern and the precedent for the default. `Blend` must stay byte-identical — figures are pinned against it. Lands *before* the measurement because the defect is structural and provable from two lines, unlike #247's, which was empirical; the control survives at 1× rather than by leaving the defect shipped |
| 6.2.5 | Contract Defects | #350, #328, #360 | **complete** 2026-08-25 — added and shipped the same day across three PRs: #350 in #372 (breaking: `IBm25Index.Search` takes the metadata filter, so the BM25 arm of client-side hybrid stops ignoring it), #360 in #373 (a benchmark run whose query selection is empty now fails instead of passing on zero queries and printing `NaN`), #328 in #374 (`KNearestNeighborsCount` no longer set below Azure's own default). **#328 stayed open**: the commit title names it, but the merged fix was `KNearestNeighborsCount` and the semantic ranker it actually asks for was split off pending the score-scale decision — a commit naming an issue is not evidence the issue is done |
| 6.2.6 | Package Boundaries | #339 | **complete** 2026-08-25 — added and shipped the same day in #376. `SqliteAuditLog` moved to a new `Rag.NET.Security.Audit.Sqlite` package and `UseAuditLog()` was removed rather than kept as a runtime check, so "auditing configured, nothing recorded" cannot be expressed — forgetting the migration is a compile error. Package count 71 → 72 |
| 6.2.7 | Named Pipelines | #342 | **complete** 2026-08-25 — added and shipped the same day in #381. `AddRagNet(name, configure)` gives each name its own child container; the unnamed form is unchanged, which was the hard constraint. Three Criticals found in review, and four tests in the plan itself could not have failed for what they claimed |
| 6.2.8 | Requested DX and Chunking Quality | #366, #365, #353, #355 | **complete** 2026-08-25 — added and shipped the same day in #378, three of its four items: #366 (heading-only chunks skipped after the breadcrumb is recorded, so the hierarchy survives), #365 (`ChatAnswerEngine` sources move to a `Tool` message), #355 (`Failed` distinguished from `Skipped` — but fail-fast was never implemented or answered, so the issue stays open carrying a question). #353 was split out into 6.2.10 during design |
| 6.2.9 | `Umap.Fit` at Corpus Scale | #348 | **complete** 2026-08-25 — added and built the same day, in #382. Measured before changing anything: the kNN graph is **92% of `Umap.Fit`'s time and 98% of its allocation**, so the issue named the right target. Bounded k-selection replaced the full sort and the row loop parallelises above 512 rows — **5.2× faster, ~729× less allocated, Gen0/1/2 all to zero**, two runs per state on an idle machine. It also corrected two of its own issue's claims: the level-1 reduction is ~6% of the corpus tree build, not "real time", and the sort-to-selection change everyone would call the headline bought 21% on its own |
| 6.2.10 | Vector-Store Initialisation | #353 | **complete** 2026-08-25 — added and built the same day, branch `feat/353-vector-store-init`. Split out of 6.2.8 during design because it looked structural: `InitializeAsync` is not on any interface, and `ICollectionManageable`'s `CreateCollectionAsync` makes a *named* collection rather than the bound index. Neither of the two costs the design section priced had to be paid — its premise was wrong and the pattern already existed in the codebase |
| 6.2.11 | HTML Structure and a Guid Seam | #375, #371, #380 | **complete** 2026-08-25 — added and shipped the same day across two PRs: #375 and #371 in #385 (the sibling walk replaced by a document-order traversal over text nodes, plus `HtmlHrefHandling`), #380 in #386 (`IGuidProvider`, an interface rather than the abstract class the issue proposed, returning `Guid` rather than `string`). Verified on `main` by content — `HtmlHrefHandling.cs` and `IGuidProvider.cs` are both present — rather than by either PR's MERGED label |
| 6.2.12 | Dogfooding Defects — what the first external user found | — | **complete** 2026-08-26 — seven PRs from one reporter (@StefH) integrating the library into a real project, which is the dogfooding this project had not done. **One was silent data loss live at shipped defaults**: under `CleanupMode.Full` a provider listing failure was collected into `Errors` and dropped, so one failed sitemap page deleted every document behind it and the run reported success (#402 — the single `stoppedEarly` bool became a `CleanupBlocked` reason). **Two of the defects were caused by fixes earlier in the same phase** — #390's fix deadlocked Blazor (#396), whose fix still hung on host singletons (#400), fixed by forwarding lazily (#403). And the reported "lock" was an unconditional `Task.Delay(1s)` per document in `AzureAISearchVectorStore.StoreAsync`, so a 500-page ingest spent eight minutes asleep for nothing (#401). **None of the four had a test and the suite was green throughout** |
| 6.2.13 | MCP Fail-Closed | #198 | **complete** 2026-09-06 — added and shipped the same day; `Rag.NET.Mcp.AspNetCore` ships a transport that throws when auth is undecided and a `MapRagNetMcp` whose key check is an endpoint filter, so "mapped but unauthenticated" is not expressible. Previously active 2026-09-06 — a library consumer calling `AddRagNetMcpServer()` then `Server.WithHttpTransport()` gets an unauthenticated remote **write** surface; the CLI already fails closed (`Program.cs:51`) and consumers building on the package do not. #189's marker-and-throw cannot be ported — `WithHttpTransport` is the SDK's extension on the SDK's builder, invisible to `Rag.NET.Mcp`, which also refuses the ASP.NET Core dependency for stdio users. Operator decision 2026-09-06: a new `Rag.NET.Mcp.AspNetCore` package shipping a guarded transport, following 6.2.6's split precedent |
| 6.2.14 | Deletion Reaches the RAPTOR Leaves | #338 | **complete** 2026-09-07 — added and shipped the same day; the decisive guard is the DI registration, without which every purge loop iterates an empty collection while compiling and passing. Previously active 2026-09-07 — `DeleteAsync` never touched `IRaptorLeafStore`, so under the shipped default scope the next corpus build resurrects a deleted document's text as a searchable summary under `raptor://corpus-tree`, carrying no document id and therefore unremovable. `RemoveDocumentAsync` existed with zero callers. Fixed via a minimal document-scoped abstraction in `Rag.NET.Abstractions`, the pattern the five stores `DeleteAsync` already clears all use; leaves also purge on re-ingest — unconditionally, alongside BM25 in `StorageBehavior`, not as an exception to `Overwrite`'s recorded stranding: the first test showed `Overwrite` defaults to false and the common path fired nothing |
| 6.2.15 | The Corpus Tree Stops Accumulating in BM25 | #336 | **complete** 2026-09-07 — every rebuild appended another full copy of the tree's postings, unbounded, because `StorageBehavior` purged only the ingesting document's id. Fixed with `IngestionContext.AdditionalAppendOnlyPurgeIds`, registered by RAPTOR only when a tree was actually built. The issue's alternative — route everything through `RaptorTreeRebuilder` — was rejected on reading it: the rebuilder never touches BM25, which is now #487 |
| 6.2.16 | The Variance Floor Learns the Data's Scale | #337 (residue closed in 6.2.19) | **complete** 2026-09-07 — the floor was an absolute `1e-6` on data of unconstrained scale. Measured: the same geometry scaled down 1,000x **collapsed k from 3 to 1** — no tree at all — which the issue did not predict and is the worse half; that is fixed by scaling the floor to the data. The near-duplicate inflation #337 actually filed is **not** fixed: at 1/100th it is, and four of #345's cluster-floor guards go red, so the fraction is 1/1000th and the residue is pinned by a characterisation test. Closing it needs a collapsed-component rejection rule, not another constant |
| 6.2.17 | BM25 Ids Belong to the Index | #490, #487 | **complete** 2026-09-07 — asking why the rebuilder could not write BM25 found the allocator underneath it broken: ids came from a per-instance counter restarting at 0 while a persisted index reloads the ids it holds, so **every document ingested after a restart was silently unsearchable** and the two stores disagreed. `IBm25Index.Add` now assigns the id, because only the index knows which are taken; #487 falls out of it |
| 6.2.18 | The Deep Research Page Honours TopK | #475 | **complete** 2026-09-07 in #494 — `RetrievalOptions.TopK` says "after all pipeline stages" and nothing truncated: `TopK` 5 with three sub-queries returned 20, and the benchmark cell saw 1,260 against its control's 250. **The issue's own suggested fix would have made retrieval worse** — truncating the union kept its ordering, which compares scores taken against different query vectors, so the cut would have dropped the caller's best chunks for a sub-query's inflated ones. Fused by RRF instead, reusing `RrfMerger` and matching what `EnsembleBehavior`'s hybrid path already returns at this boundary. **Re-measured for $0.00 on 657 cached calls: nDCG@10 0.70219 -> 0.71913, +0.02477 -> +0.04171, the largest gain any technique has had on SciFact.** The reading inverts with it — same page size now, so it is better ordering, not a wider net. The cell's own mechanism guard had to be rewritten: it detected expansion by page LENGTH, which the cap makes constant, and reported "the loop never ran" over a run where 657 calls replayed. Reproducing the replay needs a clean checkout, unexplained, filed as #495 |
| 6.2.19 | A Fit May Contain One Lonely Component | #337 | **complete** 2026-09-07 — closes the residue 6.2.16 left. **Both the issue's predicted mechanism and its characterisation were wrong, and measuring said so.** #337 expected near-identical points to survive as collapsed PAIRS needing per-component variances plumbed into the rejection path; the winning fit contains no pairs — sized 4,1,1,1,2,1,6,1,2,1, it is carried by **six singletons**, which the share test tolerated because six of twenty is not most. The variance plumbing was built, measured to be unnecessary, and reverted; a variance test could not have worked anyway, since on two tight blobs the legitimate components are also pinned to the floor. k also did not track the ceiling forever as filed — it saturated at 11. Fixed by counting lonely components, at most one, the boundary the outlier case already justified: k 11 -> **7 and stable across `maxK` 10, 15 and 19**, so the caller's ceiling no longer chooses the answer. One mutation survived — deleting the empty-component rejection changes no test — reported as #498 rather than covered by a guard that would pass either way |
| 6.2.20 | The Airtable Benchmark Discrepancy, Explained | #207 | **complete** 2026-09-08 — the 4-5x was a RECORDING error, not a regression, and nothing needed re-recording. `b202d5ec` published one connector in **two harness modes**: the three `Airtable — *` rows carry `[GlobalSetup]` numbers, the Shared Ingestion `Airtable` row carries `[IterationSetup]` numbers. Measured on unchanged code — the mode alone is worth **6.9x** (21.9 μs against 149.9 μs), and the `[GlobalSetup]` figures reproduce that commit's PARENT to within 0.7-6%. **Three of the issue's own claims were wrong**: the rows *were* re-run (`DeltaWithFilter` allocation moved ten bytes, 48.53 -> 48.54 KB); its equivalence check had only ever been made at HEAD, not at the commit; and its step 1, "check out `b202d5ec` and re-run", **is not executable** — floating `Version="0.*"` refs now resolve today's ValueObjects generator, whose emitted `ToString` collides with the April source. Settled instead by an invariant needing no rebuild: both rows call the same factory, so their ratio is immune to provider, SDK and machine drift — yet it went 5.4 -> 1.015 with allocation identical to the byte. A guard now enforces that equality, because **no test read `benchmarks.md`**, which is also how a 1 μs transcription slip survived (121.0 -> 122.0, verified against the artifact's 121,986.667 ns) |
| 6.2.21 | A Failing Vision Model Says So | #497 | **complete** 2026-09-08 — a provider failure surfaced as `ArgumentOutOfRangeException: Unknown ChatFinishReason value` from a validation helper inside a transitive dependency: indistinguishable from a genuine argument bug in the caller's own code, and named in no contract this library publishes. Now translated to `VisionDescriptionException`, carrying the file name, preserving the inner, and **not** swallowing — an empty description would ingest as a document with no content, indistinguishable from an image the model genuinely found nothing in. Tests run without a network, which is the point: the live test failed twice in one afternoon with two different causes. **The issue's own severity claim was wrong** — `PipelineIngestor` catches per document, so the blast radius is one document, not the run. **But checking that found the larger defect**, filed as #504: the ingestor's catch-all maps every non-parser failure to `RagError.StorageFailed`, whose own documentation scopes it to `IVectorStore`/persistence — so a rate-limited vision model at parse time sends an operator to inspect a healthy vector store. Not fixed here: it needs an API decision (does `RagError` gain a case?) that is the operator's |
| 6.2.22 | Why the Empty-Component Rule Stays | #498 | **complete** 2026-09-08 — a mutation that survived #337's run: deleting `IsDegenerateFit`'s empty-component rejection changes no test. Answered by measurement over **840 synthetic datasets** built to provoke it — tight blobs given more components than they can fill, exact duplicates, scale extremes, and a diffuse background where a broad component can hold weight while being no point's argmax. **4,131 of 9,280 candidate fits (44.5%) contain an empty component, and disabling the rejection changes `SelectK` on none of the 840.** So it is neither unreachable nor load-bearing: BIC declines those fits itself, since an empty component adds `2d+1` parameters and no likelihood. **Neither of the issue's two proposed outcomes was right** — it is not belt-and-braces against a case that never happens, and there is no dataset to turn into a test. What it actually buys is the `continue`: skipping `ComputeLogLikelihood` for nearly half of all candidates. Kept for cost, documented with the figures, and **no test added on purpose** — deleting it is invisible through every public surface, so any guard would pass with the rule removed |
| 6.2.23 | A Model Failure Is Not a Storage Failure | #504 | **complete** 2026-09-08 — `PipelineIngestor`'s catch-all mapped every non-parser failure to `RagError.StorageFailed`, whose own remarks scope it to `IVectorStore`/persistence, so a rate-limited vision model at parse time sent an operator to inspect a store that had not been asked to do anything. Adds `RagError.ModelCallFailed` and `ModelCallException`, the marker the ingestor maps — **breaking for exhaustive switches, taken now because pre-1.0 is when that is cheap** (operator's call). **The scope was far smaller than the issue claimed**: of the ingestion-path model callers only `RaptorIngestionBehavior` and `CommunityDetectionBehavior` propagate at all; the rest already catch locally and were never misclassified. **And wrapping those two was wrong, which the sweep caught rather than review**: their `IChatClient` is often a cache opened refuse-on-miss that throws *instead of* calling the model, so the wrap relabelled a deliberate refusal as a model failure and broke two guards. Reverted. The same hazard applies to `BudgetExceededException` — wrapping it would invite the retry a spend limit exists to prevent — so the vision parser now rethrows both it and cancellation untouched |
| 6.2.24 | Keyed Chunk Lookup on PgVector | #318 (1 of 7) | **complete** 2026-09-08 — GraphRAG local search picks its Sources by graph provenance, so it needs a keyed read; only `InMemoryVectorStore` had one, leaving **an empty Sources section and half a 12,000-token budget unspent on every remote store**. PgVector now matches pairs in the database — `unnest` over two arrays zips them, so one statement with two parameters serves any key count, where an `IN ((..),(..))` list would emit different query text per count and defeat the plan cache. Six tests against real PostgreSQL; **three mutations each caught by their own test, and the one that matters is `chunk_index >= 0`** — the unsigned assumption the issue warns about, which every other test still passes. **The issue's blocker was already disproved earlier today**: it claims these backends cannot be exercised without accounts, and all seven run locally. **Qdrant is not a copy of this**: `CreatePointId` returns `Guid.NewGuid()`, so point ids are random and its lookup must be a payload filter rather than an id fetch — recorded rather than rushed |
| 6.2.25 | Keyed Chunk Lookup on Qdrant | #318 (2 of 7) | **complete** 2026-09-08 — and it confirmed the design concern that kept it out of 6.2.24. **Qdrant cannot answer this by point id at all**: `CreatePointId` returns `Guid.NewGuid()`, so a point's id carries no relation to its `(document_id, chunk_index)` — the identity exists only as payload, and the lookup is a `Scroll` over a filter rather than an id fetch. **The pairs are matched as pairs by nesting**: one `must` of both fields per key, inside a single `should`. Flattening those conditions is the natural mistake and is mutation 1 of 3, caught. Six tests against a real Qdrant, green first run. Also pulls the payload→chunk mapping into one `MapChunk`, because search and lookup read different point types (`ScoredPoint`, `RetrievedPoint`) off the same fields and two copies would drift on what a stored chunk is. **Twice now the mechanism has differed from what the previous backend suggested**, so the remaining five are read individually rather than translated |
| 6.2.26 | Keyed Chunk Lookup on Redis | #318 (3 of 7) | **complete** 2026-09-08 — a third distinct mechanism, and the inverse of Qdrant's: a chunk's Redis key **is** its identity (`prefix + documentId + ":" + chunkIndex`), so the lookup is a direct hash read that never touches RediSearch. Reads are issued together and awaited together, which StackExchange.Redis pipelines into roughly one round trip. It also sidesteps the TAG escaping the search path needs — a direct key read parses nothing, pinned by a test using a document id containing `:` and `-`. Seven tests against real Redis; three mutations caught, and **the unsigned-index one is again caught only by the negative-index test** — three backends, same result. **Found rather than fixed: `RedisVectorStore` persists no metadata at all** (only document_id, chunk_index, text, embedding), so neither search nor lookup can return any. Asserted rather than skipped, so the limitation is visible and the test fails the day `StoreAsync` starts storing it |
| 6.2.27 | Keyed Chunk Lookup on Weaviate | #318 (4 of 7) | **complete** 2026-09-08 — a fourth mechanism: a GraphQL `where` of Or-composed And pairs, built as its own query because a keyed read has no vector, no hybrid argument and no `_additional` to select. **Mutation testing found a MISSING guard, not a passing one**: removing the document-id escaping from the concatenated query survived all eight tests, because none of their ids contained anything needing escape — a malformed query at best, an injected one at worst. Added `ADocumentIdContainingGraphQlSyntaxIsFoundAnyway`, which now catches it. The escaping itself was right; the tests simply never verified it. Also pins that `field` tokenization keeps `doc-1` from matching `doc-2` through their shared `doc` token. **Four backends in, the unsigned-index mutation is still caught only by the negative-index test, every time** |
| 6.2.28 | Keyed Chunk Lookup on Pinecone | #318 (5 of 7) | **complete** 2026-09-08 — a fifth backend and the second of the derived-id shape: `RecordId` composes `documentId:chunkIndex`, so the lookup is a `Fetch` with no query and no filter. **The interesting constraint is directional**: `BuildChunk` refuses to recover identity from a record id because a document id may itself contain the `:` separator, so ids are CONSTRUCTED here and never parsed — safe one way, not the other, and the identity still comes back out of metadata. Pinned by a test using a document id containing two colons, which catches the mutation that parses the id instead. Seven tests against Pinecone Local, sharing one index because the emulator allocates a data-plane port per index from a range of ten. Three mutations caught; **the unsigned-index one is again caught only by the negative-index test — five for five** |
| 6.2.29 | Keyed Chunk Lookup on Chroma | #318 (6 of 7) | **complete** 2026-09-08 — the one that needed a new endpoint, which is why it was not folded in with Redis three PRs earlier. Its record id is derived identically (`documentId:chunkIndex`) so it LOOKS like the same shape, but the client carried only `/query` and `/delete`, and `/query` cannot serve a keyed read at all. Adds `/get` to `IChromaApi` with its own request/response DTOs — **separate from the query response because `/get` returns FLAT arrays where `/query` nests one row per query embedding**; reusing the query type would compile and then read the first record's fields as a whole row. Same directional constraint as Pinecone: ids constructed, never parsed, identity from metadata. Seven tests against real Chroma, green first run; four mutations each caught. **Six of six on the negative-index test being the only thing that catches an unsigned implementation.** Only Azure AI Search remains |
| 6.2.30 | The Azure AI Search Key Carries Identity | #318, #517 | **complete** 2026-09-08 — scoping the last backend found a worse defect than the missing lookup: **`StoreAsync` assigned `Guid.NewGuid()` as the document key, so `Upload` could never replace and re-ingest DUPLICATED every chunk** — measured on the simulator, one chunk stored twice left two searchable, and no test stored anything twice. It is also why the lookup was blocked: with no identity in the key it would have to filter on `chunk_index`, which is declared without `IsFilterable`. **Deriving the key fixes both**, and turns the lookup into a `GetDocument` needing no OData filter — which is what makes it verifiable against a simulator that implements filters incompletely. Key is `Base64Url(documentId + newline + chunkIndex)`: Azure permits only letters, digits, `_`, `-`, `=`, so raw concatenation fails outright on an id with a slash or non-ASCII text. Five mutations caught, including that raw concatenation — **the 'simplification' that passes every test using a well-behaved id**. Breaking: GUID-keyed documents are not addressable under the new scheme, so an existing index needs recreating and re-ingesting (operator's call) |
| 6.2.31 | What Redis Never Stored, It Cannot Return | #513, #521 | **complete** 2026-09-09 — designed, built and reviewed the same day; 47 tests where the package had 16. **The whole-branch review found the defect no per-task review could see:** `HashSetAsync` merges, so re-ingesting a chunk that dropped a declared key left the old `md_*` field indexed and a filter matched a chunk whose metadata no longer carried it — two tasks each correct in isolation, the seam between them wrong. **Scoping it found a wrong-results defect rather than a missing feature:** `SearchAsync` never reads `MetadataFilter`, and `VectorStoreBehavior` is terminal, so a filtered search against Redis silently returns unfiltered results — the only one of seven stores that ignores it, and no test asserts any remote store honours it. Two design decisions were reversed by their own evidence: the corrupt-blob posture (the tolerant majority turned out to be a pre-review default that a 2026-07-25 Weaviate review deliberately replaced — filed as #521), and two RediSearch TAG defaults that would have broken matching silently, a comma splitting a value into two tags and case folding against an ordinal contract. Added 2026-09-09, from 6.2.26's own finding. The Redis keyed lookup was built and works, but `StoreAsync` writes only `document_id`, `chunk_index`, `text` and `embedding`, so **neither search nor lookup can return metadata on this backend and neither of them fails** — both hand back an empty dictionary, indistinguishable from a document that carried none. 6.2.26 asserted the limitation rather than skipping the test, so **that test is expected to go red when the store changes; its failure is the entry point, not a regression**. Two questions the design owes an answer before code: how metadata is encoded in a flat hash whose field-name space is shared with `text` and `embedding` (a metadata key named `text` must not overwrite the chunk), and whether it becomes filterable through RediSearch — #513 asks for it to be *returned*, and the TAG escaping the direct read sidesteps applies again the moment it is queried |
| 6.2.32 | A Corrupt Blob Is Not an Empty One | #521 | **complete** 2026-09-09 — **the posture that won had no test**: Weaviate has thrown since 2026-07-25 by deliberate review decision and nothing covered that path, found while checking the two sites that already threw. One shared `DeserializeMetadataOrThrow` now owns the failure and **zero callers of the raw deserialiser remain outside the serializer**, so the tolerant shape is no longer the easy one to write. Eight mutations, eight red. No upgrade hazard: null and empty already deserialise to Success-with-empty, so only genuinely malformed JSON can reach a throw. Breaking, pre-1.0, deliberately. Added 2026-09-09 from 6.2.31's own finding. **The issue says three stores; scoping found six sites across five components** — it missed `SqliteBm25Index` and `SqliteDocumentStore`, and the latter has two, one of them reading tags rather than chunk metadata. The split is six-to-two, and the two that throw (Weaviate, Redis) are the only ones ever reviewed on the point; the six are a pre-review default from a April serializer migration. Small and safe because the missing-versus-corrupt distinction already lives in the serializer: a null or empty field deserialises to Success-with-empty, so only genuinely malformed JSON can reach a throw. The design questions are whether every site wants the same answer — `SqliteBm25Index` reads inside a search loop, where one corrupt row would fail a whole query — and whether the helper belongs in `MetadataSerializer` so a seventh site cannot be written the swallowing way |
| 6.2.33 | A Fused Score Is Not a Similarity | #530 | **complete** 2026-09-09 in #531 — `IHybridSearchable` gained a defaulted `HybridScoreScale => OpaqueRanking`, and Azure AI Search and Weaviate stopped applying a similarity-shaped `MinScore` to their backend-fused hybrid scores. **The severity was corrected mid-design and the record says so**: `EnsembleBehavior.CanDispatchNatively` already requires `MinScore is 0.0`, so this was never a live defect for pipeline users — it is a direct-caller and API-clarity fix. **The mutation sweep's survivor was a control mutation on a path the phase does not touch**: Azure's *dense* `MinScore` had no test at all, while Weaviate's was already covered. And the whole-branch review found `IHybridSearchable`'s own summary contradicting the member it had just gained. #328 split out to 6.2.34 on verifiability |
| 6.2.34 | The Semantic Ranker, and the Simulator That Lies About It | #328 | **complete** 2026-09-10 in #536 — opt-in, off by default, disabled path byte-identical. **The mutation sweep inverted two of its own predictions**, and both are the record: row 6 (leak the ranker into `HybridSearchAsync`) was flagged as the one *nothing may catch* and failed two existing tests, one of them 6.2.33's; row 7 (apply `MinScore` on the ranked path) survived and **cannot be closed by any local test** — the line is *unreachable*, not untested, because the guard throws whenever `RerankerScore` is null and the simulator never returns one. That makes the account-blocked surface wider than the design first claimed: not only *does Azure's ranker reorder*, but everything downstream of the guard. The design's §5 was rewritten from that result and `<VerifiedByReason>` says so. **The one real gap the sweep found was in the plan's own test code**: a `k` boundary tested only from far outside it (10 vs 50) left 49 wrongly accepted under a mutated threshold — the exact value the guidance is about. Pre-push review PASS with two warnings, both fixed before the PR; the second, a fixed `Task.Delay` reintroducing a pattern the test project documents as removed, was load-bearing and would have failed the test reporting the guard as broken. Added 2026-09-09, split from 6.2.33 because it cannot be verified locally. **Measured: the Azure AI Search simulator accepts a semantic index configuration and `queryType: semantic`, returns HTTP 200 with results, and returns no `rerankerScore` at all** — a test written the obvious way would pass whether or not semantic ranking happened. Ships with a guard that throws when the ranker is requested and no reranker score comes back. Opt-in per instance because semantic ranking reshapes the dense path's score, whose scale must be constant for the instance's lifetime. Account-blocked for verification: Basic tier, billable, region-limited |
| 6.2.35 | A Filter That Filters Nothing | #529 | **complete** 2026-09-10 in #540 — designed, built and reviewed the same day. **Four mutations, four caught, and the phase's own prediction about one was wrong**: row 4 swaps the condition from `VSTestTestCaseFilter` to `TestingPlatformDotnetTestSupport` (the tidy a reader is most likely to make, because it makes the condition match the message) and was called *behaviourally equivalent today*. It is not equivalent in any respect — the condition asks *was a filter passed*, the property asks *does this project use MTP* — so swapping them makes the guard fire on **every unfiltered run**. The reasoning conflated a coincidence of scope with equivalence of meaning; the note is struck through rather than deleted. **Row 3 validated the structural test exactly as argued in advance**: deleting `BeforeTargets` left both behavioural tests green and the guard entirely inert, because invoking a target by name bypasses the hook. **The review found a hang-shaped risk inside the guard for a property that exists because of a hang** — the helper read one redirected stream to the end and then the other, and waited unbounded; `TestingPlatformDotnetTestSupport` is here because of #275, a deadlock that hung 2 of 4 runs before entering test code. The repository held both the weaker pattern and the better one and this branch had reached for the weaker; fixed per `CliProcessTests.RunAsync`, and all four rows re-run because rewriting a catching test invalidates its row. **Two things only running it found**: the analyzer rejects `==` on strings, and an XML comment cannot contain a double hyphen — awkward in a file whose subject is a flag spelled with one. 98 passed / 2 pre-existing skips, up from 95. Added 2026-09-10. **The issue undercounts by nearly 4x**: it says one cell means all ~70; measured 267 (149 passed, 118 skipped) from a `--filter` naming one class, and **149 execute for real with no env vars set**. Its scope claim does hold — one project sets `TestingPlatformDotnetTestSupport` — checked rather than assumed, because 6.2.32 found #521 naming three stores when there were six sites. **The platform already detects this and declines to act**: `_ValidateVSTestProperties` tests `VSTestTestCaseFilter` and raises MTP0001 as a *warning*, then runs everything. The phase changes a severity, not a detection. **It is the milestone's own defect shape one layer up** — code that succeeds while doing nothing — and it has already produced a wrong finding, during #495, where self-query prompts were read as deep-research ones: the measurement was right and the attribution was not. **Guard goes repo-wide and self-arming, against the issue's own proposal** — a new root `Directory.Build.targets` hooked to `InvokeTestingPlatform`, which exists only under MTP, so it arms for any future adopter; same reasoning as 6.2.32 making the tolerant shape unwritable. `Error`, not warning: MTP0001 is already a warning and is already scrolled past. **Prototyped and proved before the design was written**, three controls — the decisive one being a filtered *non*-MTP project still narrowing 6 of 97 with the guard silent, which is what shows a repo-wide file leaves the other sixty-odd projects alone. **Fully verifiable locally**, the first phase since 6.2.30 with nothing account-blocked. One open wording question is named: the message states a cause the condition does not check |
| 6.2.36 | The Ranker Belongs Where the Text Is | #539 | **complete** 2026-09-10 in #545 — planned, built, reviewed and merged the same day the issue was filed. **The plan-writing pass found two design errors before a line was written, and one was in this issue's own write-up**: §4 predicted the throw would surface the resilience gap "immediately and painfully — resilience plus ranking throws on every query", when the throw is conditioned on `VectorStore is IHybridSearchable` and a decorator hiding that interface is precisely what §4 had just finished describing. So under resilience the throw *never fires* and the caller gets correct, unranked, silent results — **the section predicted the opposite of the defect it had just described, using the mechanism it had just described**. Answered with a warning rather than a throw, because `IVectorStoreDecorator` deliberately exposes only `InnerStoreType` and only the instance would know whether ranking is on. **The second error was a silent path nobody had named**: `UseHybridSearch = false`, where a caller who enables the ranker and never sets it per query would get dense search forever — 6.2.34 threw for that caller, so the fix would have *introduced* an observability regression. Now the first of four named blocking conditions, with the refusal moved above the early return to reach it. **#544 filed** for the resilience gap per the design's "file it, do not fix it here", and the throw message names it. **`IScoreScaleAware` removed from the store** (the question §2 deferred): with the ranker off the dense path the property returns `Similarity` unconditionally, which `ScoreScale`'s own remarks define as the meaning of the interface's *absence*. The docs defended implementing it unconditionally on grounds that argued unconditional-over-conditional and said nothing about implementation-over-absence — and whose premise, that the value depends on a constructor option, is what this phase deletes. Two 6.2.34 tests went with it, one asserting *the dense path declares `OpaqueRanking` when the ranker is on*: the precise claim #539 is about, pinned as a test. **The sweep ran nine rows and its survivor became a test.** Row 2 split while running it and the halves differ: reintroducing 6.2.34's defect *in full* is caught, but setting `QueryType = Semantic` on the dense path **without** also expecting a reranker score passed every test in the repository — the score returned is an ordinary similarity, so the range assertion holds and the guard never fires because nothing asked it to. A plausible half-move back, sending Azure a semantic query with no search text, and **this phase's own defect shape** — so the dense request is now asserted *on the wire* never to mention ranking at all. The opposite call from 6.2.34 row 7 and 6.2.35 row 4, where survivors were recorded rather than tested; those were unreachable or pinned a coincidence, and this was neither. **Rows 4 and 5 share one catcher** (coverage-shape finding, not a pass), and **row 8 was caught only structurally while all 1496 pipeline tests stayed unmoved** — predicted, and positive evidence the `IScoreScaleAware` removal was behaviour-preserving. **Three things only running it found**: NSubstitute returns `string.Empty` for an unconfigured `string` property, so `{ NativeOnlyCapability: { } }` matched `""` and six pre-existing tests failed with a refusal reading "is configured for , which only its native hybrid query performs" — blank now counts as no declaration, and the six passed untouched; `MA0051`'s 60-line cap forced both guards into named methods; and `PackageValidation` needed `artifacts/packages` **cleared** before repacking, since packing over it left both generations present and turned one failure into three. **Pre-push review PASS** (`docs/pre-push-review-2026-09-10-1921.md`), one warning found and fixed before the PR — **and it was on the hot path**: the new decorator warning fired on *every* hybrid query from a `[Singleton]` behaviour, for a condition that is a permanent property of the registration. Fixed with an `Interlocked` guard, pinned by a test, mutation row 7 re-run after. Suites: AzureAISearch 45 → 49, `Rag.NET.Tests` 1487 → 1497, Resilience 105, Memory 3, Weaviate 30, RepoConventions 98, PackageValidation 23; whole solution clean at 158 projects, 0 warnings, so the interface removal breaks no consumer. **Still account-blocked past the guard**, unchanged from 6.2.34: the simulator accepts `queryType=semantic`, returns 200 and returns no `rerankerScore`, so the throw is testable locally and the ranking is not — @StefH asked on #539 to confirm real reranker scores against their Basic-tier resource. Added 2026-09-10. **6.2.34 put the feature on the one path that cannot carry it, and it survived three hours**: #536 merged 10:02, @StefH filed #539 at 11:30 against a real Azure resource. Semantic ranking needs query text; `IVectorStore.SearchAsync` takes an embedding and a `SearchOptions` of `TopK`/`MinScore`/`MetadataFilter` — **no text, by interface contract** — so the dense path cannot rank on any tier in any region. **The guard is why this is an issue and not a silent corruption**: without 6.2.34's throw the reporter would have received ordinary cosine similarities relabelled as reranker scores, declared `OpaqueRanking`, with `MinScore` skipped. It caught a defect its own author did not, and the `<VerifiedByReason>` naming everything past the guard as account-blocked was vindicated within hours. **The excluded path was right all along and the move is small**: `HybridSearchAsync` already takes `textQuery`, `HybridScoreScale` is already `OpaqueRanking` from 6.2.33, and `MinScore` is already 0.0 there, so no scale change is needed. **Decided: `EnsembleBehavior` throws when ranking is on and the native path is unreachable** — its dispatch needs `EnsembleOptions is null`, `MinScore is 0.0` and no SPLADE arm, and violating one silently fuses client-side with no ranking and no error, the same defect family one layer up. **A fifth condition predating the ranker, to be filed separately**: `ResilientVectorStore` does not implement `IHybridSearchable` and `EnsembleBehavior` probes the decorated store, so enabling resilience silently disables native hybrid dispatch today for every store that supports it — the repository solved this once for `ISparseSearchable` via `ResilientSparseVectorStore` and never for hybrid. Not a revert: only the path was wrong |
| 6.2.37 | The Decorator That Hid a Capability | #544 | **complete** 2026-09-10 in #549 — filed, scoped, built and merged the same day. **The mutation sweep found the defect's own shape one level up, and the plan predicted it wrong**: dropping `NativeOnlyCapability` forwarding was caught by the resilience unit test and *not* by the retrieval-level refusal test, which **cannot** catch it — that test's forwarding decorator is a fake forwarding the member itself, never touching `ResilientHybridVectorStore`. So one suite proved the *contract*, the other the *delegation*, and nothing proved the real decorator satisfied the real contract: two layers each correct in isolation that did not compose, which is exactly what #544 is. Closed with `ResilientHybridDispatchTests`, the only place both packages are referenced; the row re-run against it is caught at both layers. Seven rows, all caught, including a control proving 6.2.36's warning did not become dead code. **`Create` became a switch on the capability *pair*, not a chain of `is` checks** — the chain is what produced the bug, answering the first capability it recognised and never asking about the second; the tuple has no fifth case, so a future capability forces an edit rather than a silent drop. A store that is both sparse and hybrid is refused with `NotSupportedException` rather than decorated as one of them. **One warning recorded rather than fixed, and it is about this phase's own guard**: that `NotSupportedException` lives only in `Create`, and the variant's constructor is `public`, so direct construction bypasses it — consistent with `ResilientSparseVectorStore`, whose constructor has been public since it was written despite the base type's doc claiming `Create` is "the only public way to obtain this type". The base's stated invariant was already untrue of its variants; closing it means changing an existing public surface, outside #544. **Behaviour change stated rather than discovered**: resilience users with Azure AI Search or Weaviate move from client-side RRF to native dispatch through the retry pipeline. Suites: Resilience 105 → 112, `Rag.NET.Tests` 1497 → 1499, Memory 3, RepoConventions 98, PackageValidation 23, whole solution clean at 0 warnings. **Fully verifiable locally.** Added 2026-09-10. **The defect is as old as the decorator**: `IHybridSearchable` landed 2026-03-31 and `ResilientVectorStore` 2026-08-04, and `EnsembleBehavior` probes the *decorated* instance — so native hybrid dispatch has never happened under resilience, for any store that supports it. Until 6.2.36 that cost a silent downgrade; **6.2.36 raised the stakes** by putting the ranker on that path, so resilience now silently disables a feature the caller opted into, and 6.2.36's own refusal cannot catch it because it is conditioned on the very probe the decorator falsifies. **Two measurements narrowed it and both contradicted the obvious framing**: `ICollectionManageable` is *never* probed on a resolved `IVectorStore`, so the decorator dropping it is harmless exactly as its docs claim — `IHybridSearchable` is the only capability where probe-on-instance collides with decoration, one interface rather than a model; and hybrid and sparse are **disjoint today**, so the combinatorial explosion `ResilientVectorStore`'s docs warn about is real in principle and empty in practice. Both read off the interfaces and every implementer, not counted from declaration lines, which miss the three sparse subclasses. **Decided: a variant subclass, against the repo's other precedent.** A `SupportsNativeHybrid` flag would need no new class ever, but changes a public interface others implement and would default to `true` — the opposite polarity to `NativeOnlyCapability`, added to the same interface the day before, whose `null` means nothing declared. Two adjacent defaulted members with inverted polarity is a trap, and v1.0 is next. The combinatorial objection is also weaker here than it looks: it applies to *orthogonal* capabilities, and `SparseArmWouldRun` already keeps a sparse-capable store on the client path. **All three interface members must be forwarded**, and that is the half-doable part: two are defaulted, so forwarding only `HybridSearchAsync` compiles, passes the probe and dispatches natively while answering for the backend on the rest — `NativeOnlyCapability` defaulted would mean **6.2.36's refusal never fires under resilience**, restoring the capability while leaving the guard on it broken. #544 one level in; the sweep carries a row for it. **`Create` will throw for the sparse-and-hybrid combination it cannot represent** rather than silently pick one, since either pick is #544 again. **Behaviour change stated rather than discovered**: existing resilience users begin dispatching natively through the retry pipeline. **Fully verifiable locally** — the first phase since 6.2.35 with nothing account-blocked |
| 6.2.38 | What the Project Claims About Its Own Security | — | **complete** 2026-09-11 in #553 — scoped, built and merged the same morning. **The design's own verifiability claim was false, inside a document arguing for checking claims**: it said the repo-conventions documentation guards already run against `docs/guide/`. Nothing validates guide markdown — `DocumentationQualityTests` parses XML summaries under `Rag.NET.Abstractions`, `DocumentedConstraintGuardTests` reads `*Options.cs`, and the only occurrences of `docs/guide` in that project are two comments. The posture would have landed with no gate at all. **`SecurityDocumentationTests` was added in response and failed on its first run**, catching the posture naming eight of the ten security-adjacent packages: the two client packages were described generically rather than named. They gained a substantive line as a result — *they hold the API key*, and where it comes from is the consuming application's problem. The package list is derived from the filesystem, so it fails when someone **adds** a package. **Three drafted claims about authentication were wrong and reading the code caught them.** The plan expected to write that the MCP write surface is opt-in and so unauthenticated by default; it is not — `MapRagNetMcp` attaches the API-key filter to the same convention builder it maps, so *mapped-but-unauthenticated is not expressible rather than merely detected*, and it throws when the transport was never configured. `Rag.NET.Api` reaches the same end by the weaker detect-and-throw route, but on three conditions rather than one. **The real limitation was missed by both drafts**: the API key is a shared secret, not an identity — every client presenting it is indistinguishable, with no revocation short of rotating it everywhere. **Enumerating packages against their `.csproj` descriptions found a whole missing feature family, filed as #552**: `Rag.NET.Security`'s description opens with "Prompt injection defence-in-depth" and it ships six such types, and the page called "Security" never mentioned any of them — while `features.md` calls indirect prompt injection *the primary RAG security risk*. Filed, not fixed, per the phase's boundary. **The `qs` advisory is spent** — pinned at `^6.16.0` beside `serialize-javascript` and `uuid` (#177's precedent), unspent since 2026-09-07; `npm ls qs` resolves 6.16.0 on both paths and `npm run build` succeeds. The other four advisories have no upstream patch and are described rather than acted on. **No security behaviour changed, checked mechanically**: no `src/` file appears in the diff. RepoConventions 98 → **101**. A follow-up #554 carried one line the phase's own `git add` missed. Added 2026-09-11 **Recorded 2026-09-07, scheduled 2026-09-11, and the gap is the point**: item 3 of STATE.md's *"what is actually open"* list, recorded with its origin and then never placed in a phase — **the record-then-schedule rule failing in its quieter direction**, where the recording happens and the scheduling does not. It surfaced only because a session claimed no local work remained and was challenged; the contradicting list was in the file that session had already opened, two-thirds of the way down. **`docs/guide/security.md` documents features, not posture** — 320 lines on RBAC, PII redaction and the audit log, and no answer to what the project claims versus what it leaves to the consumer, across ten security-adjacent packages. **There is no `SECURITY.md`**: 71 packages live on nuget.org since 2026-08-11 with no private disclosure channel. Reporting will go through GitHub private advisories rather than publishing a mailbox. **The sharpest thing to state is that RBAC fails open** — `security.md` already says chunks without the key are world-readable, *in passing, two-thirds into a how-to*. The default is right (retrofitting RBAC would otherwise hide every previously-ingested chunk), but a consumer assuming deny-by-default has an exposure and no reason to suspect it. **Documenting the decision, not changing it.** **The dependency position is most of the value**: five Dependabot alerts, none in a shipped package's closure — `image-size` ×2 and `nltk` unpatched in the docs build and Python harness, `qs` ×2 patched at 6.16.0 and reaching only `npm start`. A user seeing "3 high" needs that argument. **The one fixable entry is folded in**, a one-line `overrides` pin matching #177's precedent, available since 2026-09-07 and unspent. **No security behaviour changes** — anything the writing surfaces gets filed, not fixed, so a security change never lands in a PR reviewed as documentation. Does not gate v1.0 |
| 6.2.39 | The Boundary Rag.NET Does Not Watch | — | **complete** 2026-09-11 in #556. **The phase's own test set was wrong and CI caught it** — the plan ran `RepoConventions` plus the docs build on the reasoning that a markdown-only change affects no other suite, and `pack-validate` failed: `DocsCodeExamplesTests` requires every C# example on a published page to resolve against what the produced packages ship, and the example named four symbols this repository neither ships nor depends on. **"No `src/` change" is not "no suite affected"**: this repository validates its *documentation against its packages*, so a docs-only change is precisely the kind that breaks packaging validation — and the repository's own note that `dotnet build` cannot reach the `pack-validate` guards was on file and went unapplied. Fixed in the order the guard asked for: its class doc warns that allowlist growth past a handful signals the wrong fix, so the example was **trimmed first** (the severity mapping is AI.Sentinel's configuration detail, not Rag.NET's ordering lesson), five failures becoming three, and the three allowlisted with the argument that makes them correct rather than tolerated — *a produced package resolving `AddAISentinel` would mean the boundary had been crossed, which is exactly what the section says is not done.* **The design understated its central instruction and the plan then overstated the consequence.** "Decorate the `IChatClient` before Rag.NET consumes it" is load-bearing: `UseCostBudgeting` and `UseFallbackChain` rewrite the DI descriptor and can only wrap what is already registered (#195). The plan predicted the resulting error would name cost budgeting *rather than* the ordering and strand the reader; **measured, that is false and the guard is better than the plan credited** — it names the ordering, explains the mechanism and states the fix imperatively. It is quoted verbatim in the section. **Two findings the probe added that neither document anticipated**: the guard fires only when `IRagPipeline` is resolved, not `IChatClient`, so a smoke test resolving only the chat client reports a broken composition as healthy; and in the correct order Rag.NET's decorators wrap *around* the monitor, which is the right nesting because the monitor then sees the prompt exactly as sent. **An earlier probe run was itself wrong and is recorded rather than deleted** — it resolved `IChatClient` instead of the pipeline, reported "no guard fired", and would have become a documented claim that the guard has a hole. **The self-recommendation was reviewed as a risk, not assumed benign**: the disclosure is a visible blockquote, the pattern is stated before any product is named, and the overlap section states the cost — prompt injection covered twice, two LLM-backed passes per query being a real expense, with advice to decide deliberately "rather than enabling both because each page recommends it". **AI.Sentinel #205 filed** for a non-idempotent `AddAISentinel` found while testing the composition: its README's own named-pipeline example builds a pipeline holding 165 detectors. Nothing entered `src/`; RepoConventions **101**, PackageValidation **23**. Added 2026-09-11 **6.2.38's posture made an omission visible**: Rag.NET's security features act at four points and all four are *before* the model is called — sanitisers at ingest and pre-retrieval, guards at retrieval, prompt hardening at assembly. **Nothing acts after.** `IConfidenceScorer` looks like it does but answers a different question — whether a sentence is *supported by the retrieved context*, failing open at `1.0` — which is groundedness, not inspection of a response for a credential the model saw in a chunk and repeated. **Documentation, not code, and that is the phase's main risk to communicate**: a reader could reasonably expect an integration package, and there deliberately is none. The composition already works because both sides sit on `IChatClient`. `SecurityPackageWeightTests` exists because one file once put SQLite on every `UseRbac` consumer; AI.Sentinel brings thirteen `ZeroAlloc.*` packages and must never enter `Rag.NET.Security`. **The example names AI.Sentinel and discloses the shared authorship**, chosen by the operator over naming-without-disclosing, generic guidance, and documenting nothing — it lands immediately after a posture section whose whole value is being believed, and an undisclosed self-recommendation found later would be read backwards onto everything above it. **The overlap is stated rather than glossed**: both do prompt-injection detection by different means, so running both is defence in depth *or duplicated cost* depending on configuration. **Measured, not assumed**: AI.Sentinel 2.0.1 builds against `ZeroAlloc.Mediator` 4.1.4 and `ValueObjects` 1.7.1 where this repo pins 5.0.1 and 2.0.5 — two majors apart, NuGet resolving higher. A throwaway `net10.0` spike forcing those pins showed 55 detectors resolve and construct and a scan runs clean of `MissingMethodException`. **What it does not prove is recorded too** — construction and the scan path, not every detector's internals — so the claim ships dated and version-named. **One spike observation reported to AI.Sentinel's author rather than chased**: a blatant injection scanned clean in a bare config with all five injection detectors registered, almost certainly a missing `EmbeddingGenerator`; it is why the section tells readers to verify against their own configuration, since registration that silently protects nothing is this milestone's recurring shape |
| 6.2.40 | The Feature Family Nobody Documented | #552 | **complete** 2026-09-11 in #561. **#552 understated its own subject**: it said the detail lived in `features.md`, but six public registration methods had **no published documentation of any kind**, and the reference entry was a **design proposal marked ✅ Done** — future tense, describing a promotion to a public `IChunkSanitiser` abstraction that had already happened, naming none of the shipped methods. `FeatureClaimTests` passed it because it asserts only that a Done section names packages existing under `src/`. **Reading the implementations found two things neither the issue nor the design knew.** `TrustLevelRetrievalGuard` treats *absent* `trust_level` metadata as `internal` — the most trusted value — so it drops nothing over an untagged corpus: **a second fail-open default**, structurally identical to RBAC's, which the posture called "the single most important default on this page" while saying nothing about trust levels. `### RBAC fails open` became `### Defaults that fail open` covering both, because documenting one while the page implied a single notable instance would have been a fresh inaccuracy introduced by the phase fixing inaccuracies. And **query sanitisation does not apply to `RetrieveAsync`** — defensible, since injection hijacks a model and that path reaches none, but nothing recorded the intent: no doc comment, no test covering that method, no page mentioning it. Documented and **filed as #559** rather than changed. **#560** filed for the other 52 ✅ Done entries. **The plan committed the failure its own constraints warn about**: it asserted `Rag.NET.Security` has no test project, two paragraphs below a constraint to enumerate suites rather than reason about which are safe to skip. It has 16 files and **104 tests**. Struck through, suite added, passed unchanged — the same class of error as 6.2.39's `pack-validate` miss, in the same week, in a document warning against it. **`pack-validate` ran locally this time**: 23/23, no allowlist entry needed since the examples name Rag.NET's own shipped types. **XML `<summary>` on all ten `Use*` methods**, not just the six — `UseRbac` and `UsePiiDetection` were equally bare in IntelliSense. **Comment-only `src/` diff, verified by `git diff`**, build at 0 warnings. Suites at baseline: RepoConventions 101, Security 104, PackageValidation 23, `Rag.NET.Tests` 1499. Added 2026-09-11 **#552 understates its own subject**: it says the detail lives in `features.md`, but measured against `RagBuilderExtensions.cs`, **six public registration methods have no published documentation anywhere** — `UseQuerySanitiser`, `UseLlmQuerySanitiser`, `UseLlmChunkSanitiser`, `UseRetrievalGuard` and `UseTrustLevelGuard` on no page at all, `UsePromptHardening` one passing mention in `extending.md`. A reader cannot discover `UseTrustLevelGuard` exists. **The reference page is a design proposal marked ✅ Done** — future tense throughout (*"layers to consider"*, *"the full fortification feature should promote this to a public `IChunkSanitiser` abstraction"*) describing a promotion that has since happened, naming none of the shipped methods. **`FeatureClaimTests` does not catch it**: it asserts only that a Done section names packages existing under `src/`, and `Rag.NET.Security` exists, so the claim passes while the body describes unbuilt work. **And 6.2.39's posture section points readers at it** — *"the reference is the place to read"* sends them to a proposal. The previous phase documented a gap and introduced a misdirection into the one page whose value is being believed, so correcting it is an obligation here rather than a nicety. **A fourth finding widened the phase by the operator's call: none of the ten public `Use*` methods carries an XML `<summary>`** — not the six, and not `UseRbac` or `UsePiiDetection` which the guide covers well; the file's 26 `///` lines are all on private helpers, and `DocumentationQualityTests` scans `Rag.NET.Abstractions` only. All ten get documented, because most people meet these methods by typing `b.Use` and reading what the IDE offers. **Comment-only `src/` change**, checked rather than asserted. **The most useful thing the section can say is in neither existing one**: `UseRbac` registers an `IRetrievalGuard`, so RBAC and the retrieval guards are the same extension point, and `IChunkSanitiser` is shared with PII redaction. **`pack-validate` runs locally before the PR**, since 6.2.39 shipped a PR that failed `DocsCodeExamplesTests` on exactly the assumption that markdown cannot break packaging. **Filed not done**: whether other ✅ Done entries are stale proposals — 53 of them, an audit rather than a chore |
| 6.2.41 | The Migration a Dependency Bump Was Hiding | #314 | **complete** 2026-09-12 in #567 — **and its own premise was falsified by testing it.** Migrating does **not** unblock #314: `TestingPlatformDotnetTestSupport` opts into the VSTest *bridge*, and MTP 2.3.3 (which `xunit.v3` 4.0.0 pulls) **removed the bridge**. Applied on top of the migration the bump **built clean** — the first green build of that change since 2026-08-18 — then produced **no test output for 77 of 77 projects**. Both `global.json` runner values were tried on SDK 10.0.401: `"VSTest"` gives the MTP error, `"MicrosoftTestingPlatform"` is **rejected by the SDK's own CLI parser**. Bump reverted; the blocker is SDK support, not this repository, and the #314 diagnosis was corrected on the PR. **Shipped anyway, on the operator's call once the justification changed** — in-process MTP avoids the VSTest adapter deadlock behind #275 and makes skip reasons visible. **Coverage proven, not asserted: 77 projects, 5336 tests, 138 skips, identical per project.** The output *format* changed (`net10.0|x64`, different spacing) and the counts did not — a text-keyed comparison would have reported 77 false differences. **The documentation damage was four times larger than scoped and mostly pre-existing**: thirteen `--filter` commands, of which **eleven had been broken since #275** made one project MTP — verified by reverting the migration and watching a documented command still fail. **6.2.35 invalidated eleven documented commands and nobody noticed for a fortnight.** All thirteen converted to the native runner, conversions executed rather than assumed. **Three are lossy and say so inline**: no filter mechanism addresses a theory data row, and the harness has no dataset env var, so BEIR commands that ran one dataset now run all of them. **A caveat found after merge and recorded rather than buried**: both sweeps ran with the BEIR cache unsourced, so ~118 of that project's 267 tests never executed under either runner. The before/after comparison stays valid — same state both times — but verification is project-granular, not test-granular, for those. Sourcing `env.sh` took `Embeddings.Onnx.Tests` from 10 skips to **0**. **Gap then closed the same day**: the benchmark project re-run provisioned under MTP gives **175 passed / 92 skipped / 0 failed**, against 149/118 unprovisioned — so **26 previously-unverified tests now execute under MTP and all 26 pass**. The residual 92 need `RAGNET_BEIR_LONG_RUNS`, API keys, or are capability-gated. Added 2026-09-12 **#314 is not a dependency bump**: a Renovate PR red since 2026-08-18, all three checks failing for one reason with no xunit API involved — `xunit.v3` 4.0.0 pulls `Microsoft.Testing.Platform.MSBuild` 2.3.3, which **on the .NET 10 SDK refuses the VSTest bridge outright**, so every test project fails to *build* its VSTest target. Renovate can never make it green, and the migration is worth doing independent of xunit v4: the deadline belongs to whichever MTP version the repo lands on. **The first diagnosis overstated it and is corrected in the design.** The #314 comment called this a "78-project migration", a "large phase", and said incremental viability "is not obvious". Ten more minutes of reading refuted both: `tests/Directory.Build.props` is imported by every test project, so the opt-in is **one property in one shared file**; and `Rag.NET.Benchmarks.Quality.IntegrationTests` has been MTP since #275 while sitting in the **fast tier `ci.yml` runs on every PR**, beside 65 VSTest projects — **mixed mode has been the status quo for weeks**. **One detail decides whether the tier census survives**: that project keeps `Microsoft.NET.Test.Sdk` deliberately (*"both workflows SELECT"* by it, per its own comment) and the partition guard counts with `grep -l 'Microsoft.NET.Test.Sdk'` — so removing the reference would drop projects from the census **while the guard still passed**, since `all` shrinks with `fast` and `docker`. **The real cost lands on developers, not CI.** 6.2.35's `RAGNET0001` guard hooks `BeforeTargets="InvokeTestingPlatform"` specifically so it **arms itself for any project adopting MTP later**, so the moment the property lands `dotnet test --filter` errors repo-wide. That is correct — the filter was silently ignored before, running every test while appearing to narrow — and the guard already prints a copy-pasteable native-runner replacement. **6.2.35 built exactly the right guard for a migration nobody had scheduled.** The phase documents the new local workflow rather than merely flipping a property. **Well guarded structurally, unguarded in aggregate**: the partition check, the assembly-existence pre-check and the RepoConventions tier invariants all hold, but nothing asserts the same number of tests ran afterwards — so per-project counts are recorded before and compared after. **Fully local and unblocked**, unlike 6.1 and 6.3 |
| 6.2.42 | Two Rules That Were Written Down and Broken Anyway | — | **complete** 2026-09-12, merged as #572 — three guards shipped, each proven by running it rather than reading its diff: `CommitMessageHookTests` executes the real hook, `SkipMessageTests` asserts exact sentences for both message branches under temp directories, `CiFailureReportingTests` counts `iconv` occurrences against `dotnet test` occurrences per workflow loop. **The Linux encoding question the design left open is now closed by evidence**: implementer and re-reviewer each built the same deliberately-failing project inside a fresh `mcr.microsoft.com/dotnet/sdk:10.0` container and read the identical `FF FE` BOM Windows produces — the `iconv` branch fires on every platform checked, and the implementation plan's stale "unverified" caveat was corrected -- the design document's own prose never made that claim, so only the implementation plan needed correcting. **Guard A's adoption is stated as untested and untestable**, not implied as enforced: the hook does nothing until `git config core.hooksPath .githooks` is run by hand. Provisioned BEIR numbers measured fresh: `Embeddings.Onnx.Tests` 141/10 unprovisioned to **151/0**; the benchmark project 154/118 unprovisioned to **180/92**. **Pre-push review PASS** — `docs/pre-push-review-2026-09-12-1359.md`, 0 blockers. Added 2026-09-12. **Not from an issue — from this repository breaking its own recorded rules three times in a day.** *Enumerate suites rather than reasoning about which are safe to skip* (6.2.40's plan then asserted a test project did not exist; it has 104 tests); *commitlint caps headers at 100 and lints every commit a PR adds* (a 104-character header then failed CI on #567, on a non-tip commit, forcing a branch rebuild); *source `env.sh` before writing "unprovisioned"* (both 6.2.41 sweeps then ran unprovisioned, skipping 26 tests that would have passed — the third occurrence, after a note saying two sessions had already done it). **The pattern is not missing rules**: each was written, in a file its author had read, twice in the same document. **Prose rules are not checked at the moment they apply.** What worked in 6.2.41 was mechanical — a count-keyed comparison catching what a text-keyed one would have called 77 differences, and `git diff | grep ^src/` proving a constraint intent could not. **Guard A**: there is **no local commit-message check of any kind** — no husky, no `prepare`, no `commit-msg` hook — so a malformed header costs commit, push, PR, wait, rewrite, force-push. **Header length only, deliberately**, since `.commitlintrc.yml` tunes `type-enum`, disables `subject-case` and turns `body-max-line-length` off for verbatim URLs, and a second implementation would drift; CI stays authoritative. Delivered as a tracked `.githooks/commit-msg` plus a one-time `core.hooksPath` line, chosen over husky-via-npm (lifecycle machinery in a docs-only `package.json`) and over a test (fires after the commit exists). **Cost stated rather than glossed: a hook does nothing until someone runs the config line.** **Guard B**: the BEIR skip is correct but its *message* cannot distinguish "corpus absent" from "corpus present and unreferenced" — the second happened three times, and sourcing `env.sh` takes `Embeddings.Onnx.Tests` from 10 skips to 0 and the benchmark project from 149/118 to 175/92. The skip will name the directory it found. **No change to which conditions skip.** **The third rule stays prose deliberately** — a guard for it would have to know which suites a change affects, the judgement the rule disciplines, so it would run everything or guess. Its actionable form is already in use. **The hook gets a test feeding it a 101-character header**, because a guard nobody tests is the thing this phase is about; **adoption cannot be tested** and the record says so. **AMENDED 2026-09-12 with Guard C, from #571**: phase 6.2.41's MTP migration removed failure detail from CI output entirely — no test name, no assertion, no stack trace, no annotations — so a flake on #570 took a full log read, a count comparison against `main` and a reproduction outside the repository, and **still could not be attributed to a test**. Reproduced with a throwaway project, so it is neither ubuntu- nor CI-specific; the detail goes to a per-project log under `TestResults/` that nothing uploads, **UTF-16LE with a BOM**, so a plain `cat` prints mojibake where `iconv` recovers it. A few lines in the failure branch `ci.yml` and `nightly.yml` already have. **Does not touch the #571 race** — makes the next occurrence legible. **By this phase's own criteria Guard C outranks Guard A**: one branch rebuild and opt-in reach, against every red build and no opt-in. **Verified by a deliberate failure, never by a green run** — a green run is how 6.2.41 shipped the regression. **AMENDED 2026-09-12 after a final whole-branch review found ten findings, all fixed on this branch**: none of Guard B's three hint sites was covered by anything that would notice its `+ …Hint()` call being deleted, on any CI runner, because a runtime composition test cannot tell "called and returned empty" from "never called" when the live hint is empty — closed with a new `RepoConventions` source-text guard, `SkipReasonWiringTests`, deterministic on any machine. `RepoConventions` is now **111/2** (was 107/2); the two BEIR-gated projects are unchanged. Full account in `.superpowers/sdd/2026-09-12-mechanical-guards-implementation/final-fix-report.md` |
| 6.2.43 | The Entry Point That Was Already Fluent | #184 | **complete** 2026-09-12, merged as #577 — shipped a documentation correction and the test that earned it. `getting-started.md` told readers to register the AI services before `AddRagNet`; **that constraint does not exist**, proven by `RegistrationOrderTests` registering both orders and asserting each container returns the exact instances registered. Added 2026-09-12, **scope reduced the same day, before any code**: the two builder methods are dropped and the phase ships the ordering test, the documentation correction and a comment on #184. The operator challenged the design as over-engineering and it did not survive — **the methods unify syntax without reducing decisions**: same client constructed, same three things to know, and the verbose part was never the registration. Against a goal of "fewest decisions to something working" the count was identical and only the punctuation moved, while the cost was a `Microsoft.Extensions.AI` reference on core plus two ways to register one service — the trap the design had rejected its own alternative for laying. **Finding kept although it decides nothing now**: `AddChatClient` is a pipeline entry point returning a `ChatClientBuilder`, not a registration helper, so a naive `AddSingleton` would have discarded `UseLogging`/`UseOpenTelemetry`/`UseDistributedCache`/`UseFunctionInvocation` silently. **#184's premise has drifted and the drift is the finding.** The issue asks for "one builder where everything is configured fluently"; the builder exists and the documented quickstart already chains — `services.AddRagNet(rag => rag.UsePgVector(…).AddPdfParser())` — with optional packages attaching through `TBuilder where TBuilder : IRagBuilder` returning `TBuilder`. Two supporting claims no longer hold: **#181 is merged**, killing the "the bump is happening regardless" argument, and **#161 is closed**. **Methodological note that changed the scope**: the extension surface was first counted by grep and two reasonable greps returned **42** and **3** for the same quantity, because C# signatures wrap across lines — the scope came from reading `IRagBuilder`, `RagBuilder` and `ServiceCollectionExtensions` instead, and no work derives from a count. **What remains is one seam**: the model and embedder are registered outside the chain as two `Microsoft.Extensions.AI` calls, and no `UseChatClient`/`UseEmbeddingGenerator` exists on `RagBuilder` — checked directly, absent rather than differently named. **The stated ordering constraint may not exist**: `getting-started.md` says to register them before `AddRagNet`, but every consumption found is `sp.GetService`/`GetRequiredService` inside a factory lambda, so order should be irrelevant — **recorded as a hypothesis and turned into a test**, since a pass deletes a doc sentence and a fail reveals a real constraint. **Two methods on the concrete `RagBuilder`, not `IRagBuilder`** — the latter is a shipped three-member abstraction external packages are generic over, so adding to it breaks implementers and buys nothing. Both take the `Microsoft.Extensions.AI` abstractions already consumed, so the dependency closure is unchanged; chaining composes in both directions and is tested by compilation. **The risk most likely to fail silently**: if `AddChatClient` wraps the client in middleware, a naive `AddSingleton` loses it and a "the client resolves" test passes either way — so the methods delegate and the check compares against what `AddChatClient` produces. **Additive despite the `breaking-change` label: every call site that compiles today still compiles.** Rejected explicitly — provider-specific `UseOpenAI(key)`, touching any existing extension, adding to `IRagBuilder`, and the options-discoverability layer. **Consequence for planning: #184 was the strongest remaining argument for breaking work before v1.0; if it closes additively that argument dissolves** |
| 6.3 | Release v1.0 | — | pending — **but its first work is already done**: 71 packages live on nuget.org at 0.1.0 since 2026-08-11 (verified 2026-08-16), so the account, key and every package ID are settled. Only the v1.0 tag remains. ~~Now gated on 6.2.3~~ — cleared 2026-08-21. Still gated on 6.1's recordings (the operator kept that gate) and 6.2.1's sweep |

## Known debt carried into this milestone

- **#247 closed 2026-08-18** — one shared store for article and graph-derived chunks, measured
  −0.043 nDCG and −0.21 answer accuracy: the largest lever any Milestone 5 measurement found, and
  the one debt this milestone opened pointing at. It resolved in the order the method prescribes.
  **Measured before it was fixed** (#274): at n=50, top-6, a `filtered` arm that dropped the
  graph-derived units after over-fetch reproduced the article-only `dense` arm to four decimals on
  both scoring rules — 0.2727 against a polluted control's 0.1364 — so the entire loss was
  *displacement*, not any cost of the graph store existing. The mechanism confirmed itself by
  accident: 46 of 50 queries hit the answer cache, which is keyed on a prompt embedding the
  context, so for those 46 the filtered top-6 was byte-identical to the article-only context.
  **Then pinned** at 0.3494 (#280). **Then shipped**, and only then: #311 hid the graph's chunks
  from retrieval results by default, #312 gave them their own store — option (b), Microsoft's
  shape, though (c) had already recovered 100%. The three options separated on evidence rather
  than on cost. *(RAPTOR indexes synthetic summaries the same way and is expected to take the same
  fix; that is why it is 6.2.1's next thread.)*
- **#239 closed 2026-08-17.** Its three findings resolved differently, which is worth recording
  because only two were code. **Point 1, the blend:** `PageRankWeight` defaulted to 0.3 while
  PageRank normalises to a mean of 1.6e-5 against cosine's 0.3–0.6, so the behaviour demoted
  precisely the chunks it had traversed to — at `w = 0` local search reproduced the candidate-set
  control on **2,255 of 2,255 queries**, so the entire −0.02761 was that default. Now 0 (#296).
  **Point 3, the discarded calls:** two graph queries awaited and thrown away, ~45,000 table scans
  per query pass, removed (#291) — and a test had asserted `Received(1)` on them, pinning the waste
  as coverage. **Point 2, that local search can only reorder and never expands the candidate set:**
  *not* changed. Measured at +0.00148 Recall@100 if the walk's findings were used; closed as a
  documented limitation, and `docs/guide/graphrag.md` now states plainly that the behaviour adds no
  candidates.
- **#176 singleton communities — answered 2026-08-26 in #405; not a defect worth fixing.** The
  numbers first: it carries 65%, and the full 609-article corpus measured 2026-08-20 gives **2,816
  singletons of 3,573 — 78.8%**. **Not a regression** — the 65% is 396 of 607 over the pinned
  **60-article** slice (#168), so the two were never measuring the same graph; 78.8% is the first
  full-corpus reading and it moves the wrong way with scale. The issue's own diagnosis stood: it is
  extraction, not clustering.
  **What settled it was reading the dropped endpoints' names rather than counting them.** 853 of
  16,403 relationships (5.20%) are dropped because an endpoint resolves to no extracted entity,
  stranding 123 entities that do have edges — and 273 + 123 is exactly the slice's 396. Those drops
  name **565 distinct** endpoints which are *not* entities the extractor missed: `content policies`,
  `tasks`, `smart plug`, `handy tool`, `film`, `ceremony`, `death` — common nouns — beside
  paraphrases of things that are extracted (`Falun Gong practitioners` next to the entity `Falun
  Gong`). **Promoting them into entities would drive the singleton share down while adding 565 junk
  nodes**, which is a better-looking number over a worse graph; the singleton count is the metric
  easiest to move without helping. Nothing was changed. Any real fix belongs in the extraction
  prompt and must be measured against retrieval. The diagnostic stays, printing the counts and a
  ten-name sample. *(**#200** usage recording, **#246** the
  Service Bus emulator lock race, and **#104** routing all closed. #104 was listed here in error;
  it closed 2026-08-10, before this milestone opened.)*
- **#297 and #300, both found by questions rather than planned, both closed 2026-08-17.** The graph
  store had **no indexes at all**, and the obvious fix did not work: an index on
  `relationships(source_entity)` was unusable while the predicate said `COLLATE NOCASE`, because an
  index's collation must match the predicate's. Fixing that meant fixing #299's collation bug first
  (#304). And community detection — a whole-graph operation — ran **once per ingested document**,
  17,648 times on the MultiHop-RAG corpus, every run but the last discarded; now debounced on graph
  growth with `GraphProjectionRebuilder` for on-demand rebuilds (#302).
- **#298 open — should the graph store support backends beyond SQLite?** Recorded answer: *not
  yet*, and weaker now than when asked. The two costs attributed to storage were a missing index
  and a per-document recompute, both since fixed without changing engines. Concurrency (one shared
  `SqliteConnection` on a singleton) is the only remaining argument, and nobody has stated that
  requirement.
- **#299 open — multilingual.** Two of its three defects fixed 2026-08-17: SQLite's `COLLATE
  NOCASE` folds ASCII only while the callers fold Unicode, so non-English entity names were one
  entity in memory and two rows in the graph (#304); and BM25 tokenised a whole CJK sentence as one
  term, so hybrid search silently degraded to dense-only for Chinese, Japanese and Korean (#305,
  which also made Vision's OCR language configurable). **What remains is not code**: there is no
  multilingual measurement, so none of this is a multilingual *claim*. MIRACL or mMARCO would fit
  `BeirDatasetDescriptor`'s existing shape.
- **#252** `SitemapDataProvider` cannot skip URLs — reported 2026-08-15 against a shipped package.
  6.2.2. **Closed 2026-08-16**: `SitemapOptions` with prefix and regex exclusions, applied to
  nested index links as well as page URLs.
- **The #300 follow-up measurement is outstanding**: the split of `BeirRunBudget`'s 22 m 18 s graph
  construction between LLM extraction, the `O(occurrences²)` description rewriting and the
  recompute. #302 should have moved that number and nobody has re-measured. It needs the
  provisioned corpus **and an idle machine** — three timing runs attempted on 2026-08-17 disagreed
  by 6× on identical inputs, so no coefficient was claimed anywhere.
- **TREC-COVID's weakest-of-the-datasets agreement** (−0.018 in ±0.02) — nothing yet looks into
  it; 6.2's parity-through-stores runs the same leg many more times and is where it would show.
- **Generation lives in the answer test class rather than the tool** (5.2.2's stated deviation).
- From Milestone 4: the Azure Document Intelligence live half, the AzureAISearch OData filter path,
  the Pinecone live sparse-write verification — all 6.1's.

## Explicitly not in scope

- **Making every feature good.** Battle-tested means measured; a feature measured and found wanting
  is a completion, as 5.2 was. *(6.2.2 is the one deliberate exception, and is scoped to named,
  reported requests against shipped packages — not to improvement in general. It exists because
  this is the terminal milestone, so a request filed against a published package has nowhere
  later to land.)*
- **Every option on every feature.** One real run per feature at its shipped defaults, differenced
  against a control, is the bar; anything above it is a phase with its own name.
- **Anything on the "Beyond v1.0: Recorded, Not Scheduled" list.**
